// initial state && objects formats

export default {

    /**
     * list of friends
     * @type {Array<friends>}
     */
    friends: [],

    /**
     * to know if state is loading for data
     * @type Boolean
     */
    isFetching: false,
};

